import { StyleSheet } from 'react-native'

import { gray600 } from '../../tokens/colors'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: gray600,
    padding: 30,
  },
})
